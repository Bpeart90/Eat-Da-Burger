<h1>Bob's Burgers!</h1>

<h2>Eat Da Burger!</h2>

<ul>
  {{#each burgers}}
    {{#unless devour}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Devoured Burgers!</h2>

<ul>
  {{#each burgers}}
    {{#if devour}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Burger of the Day!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Type:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devour">How Hungry are You?</label><br>
    <input type="radio" name="devour" value="1" checked> I can Eat<br>
    <input type="radio" name="devour" value="0"> Devour!
  </div>

  <button type="submit">Add a Burger</button>
</form>
